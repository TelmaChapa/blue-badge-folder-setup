// State is information that the component stores and keeps track of. State changes over time.
//State can be primitive (booleans, number, strings)or it can be complex data types (arrays, objects, functions)

const state = ( => {
    return (
        <div>
            
        </div>
    )
})