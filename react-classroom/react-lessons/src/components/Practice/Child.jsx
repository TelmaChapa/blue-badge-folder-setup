const Child = (props) => {
    return ( <div>
        <p>Hello from Child!, {props.name} is living in {props.city} and enjoys the Summer activity of {props.summerActivity}</p>
        </div> );
}
 
export default Child;